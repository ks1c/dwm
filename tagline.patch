diff --git a/config.def.h b/config.def.h
index 1c0b587..70ada20 100644
--- a/config.def.h
+++ b/config.def.h
@@ -21,6 +21,10 @@ static const char *colors[][3]      = {
 /* tagging */
 static const char *tags[] = { "1", "2", "3", "4", "5", "6", "7", "8", "9" };
 
+/* draw tag line */
+static const int drwtagline = 1;
+static const int lnthic     = 2;
+
 static const Rule rules[] = {
 	/* xprop(1):
 	 *	WM_CLASS(STRING) = instance, class
diff --git a/dwm.c b/dwm.c
index 4465af1..67abc1a 100644
--- a/dwm.c
+++ b/dwm.c
@@ -722,6 +722,8 @@ drawbar(Monitor *m)
 			drw_rect(drw, x + boxs, boxs, boxw, boxw,
 				m == selmon && selmon->sel && selmon->sel->tags & 1 << i,
 				urg & 1 << i);
+		if (m->tagset[m->seltags] & 1 << i && drwtagline)
+			drw_rect(drw, x, bh - lnthic, w, lnthic, 1, 0);
 		x += w;
 	}
 	w = blw = TEXTW(m->ltsymbol);
